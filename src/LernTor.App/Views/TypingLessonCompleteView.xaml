<UserControl x:Class="LernTor.App.Views.TypingLessonCompleteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:LernTor.App.Localization">
    <Grid Background="{StaticResource BackgroundBrush}" Style="{StaticResource FadeInOnLoad}">
        <Border Style="{StaticResource CardBorder}" MaxWidth="600" Padding="40">
            <StackPanel HorizontalAlignment="Center">
                <!-- Ergebnis -->
                <TextBlock Text="{Binding Passed, Converter={StaticResource BoolToResultEmojiConverter}}"
                           FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,16" />

                <TextBlock Text="{Binding Passed, Converter={StaticResource BoolToResultTitleConverter}}"
                           Style="{StaticResource HeadlineText}" FontSize="36" TextAlignment="Center" Margin="0,0,0,8" />

                <TextBlock Text="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[Typing_LessonCompleteMsg]}"
                           Style="{StaticResource SubtitleText}" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,24" />

                <!-- Stats -->
                <Border Background="{StaticResource TileLavenderBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border Background="{StaticResource TileMintBrush}" CornerRadius="6" Padding="12,4" Margin="0,0,16,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✅ " FontSize="16" />
                                <TextBlock Text="{Binding Accuracy, StringFormat='{}{0:P1}'}" FontWeight="Bold" FontSize="20" Foreground="{StaticResource PrimaryBrush}" />
                                <TextBlock Text="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[Typing_AccuracyLabel]}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="4,0,0,0" />
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource TileMintBrush}" CornerRadius="6" Padding="12,4" Margin="0,0,16,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🚀 " FontSize="16" />
                                <TextBlock Text="{Binding Wpm, StringFormat='{}{0:F0}'}" FontWeight="Bold" FontSize="20" Foreground="{StaticResource PrimaryBrush}" />
                                <TextBlock Text="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[Typing_WPM]}" FontSize="20" Foreground="{StaticResource PrimaryBrush}" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource TileMintBrush}" CornerRadius="6" Padding="12,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⏱️ " FontSize="16" />
                                <TextBlock Text="{Binding Elapsed, Converter={StaticResource TimeSpanToString}}" FontWeight="Bold" FontSize="20" Foreground="{StaticResource PrimaryBrush}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Sterne -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,24">
                    <ItemsControl ItemsSource="{Binding StarsList}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="⭐" FontSize="32" Margin="4,0" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                    <Button Content="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[Typing_Retry]}"
                            Style="{StaticResource RoundedButton}" FontSize="20" Padding="24,12"
                            Command="{Binding RetryCommand}" Margin="0,0,16,0" />
                    <Button Content="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[Typing_Continue]}"
                            Style="{StaticResource RoundedButton}" FontSize="20" Padding="24,12"
                            Command="{Binding ContinueCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>